<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.xofome.xofome">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ListaPedidoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityAdd" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_lista_pedido"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DescricaoProdutoActivity"
            android:label="@string/title_activity_descricao_produto"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>